version: '3.8'

services:
  space-invaders:
    build: .
    container_name: space-invaders
    ports:
      - "3000:3000"
    volumes:
      - space-invaders-data:/app/data
    environment:
      - NODE_ENV=production
    restart: unless-stopped

volumes:
  space-invaders-data:
    driver: local
